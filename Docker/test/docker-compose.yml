version: '3'

services:
  db:
    image: mysql
    ports:
      - 3306:3306
    networks:
      - app

  app:
    image: jhonnypz/app-react
    networks:
      - app

  proxy:
    image: nginx
    ports:
      - 8080:80
    networks:
      - app

networks:
  app:
